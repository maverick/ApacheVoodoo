package app_newstyle::C::a::controller;

use strict;
use warnings;

use base("Apache::Voodoo");

sub handle {
	my $self = shift;
	my $p    = shift;

	return {
		'a_controller' => 'a controller'
	};
}

1;
